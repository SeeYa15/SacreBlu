var jsonObject = {
    Words:{Letters:[
        {cc:[
            {description:"if before e, it will sound like X", pronoun:"Think of it like an axe"}]},
        {j:[
            {description:"if simple word like 'je'", pronoun:"Jöööööööö"}
        ]}
        ]}
}

export default jsonObject;

